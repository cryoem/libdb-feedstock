channels:
  sources: [defaults, conda-forge]
  targets:
    - [cryoem, main]

github:
  user_or_org: cryoem
  repo_name: libdb-feedstock

provider:
  linux: circle
  osx: travis
  win: appveyor

azure:
  build_id: blank
